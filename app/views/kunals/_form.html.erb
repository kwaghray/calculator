<%= form_for(@kunal) do |f| %>
  <% if @kunal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kunal.errors.count, "error") %> prohibited this kunal from being saved:</h2>

      <ul>
      <% @kunal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :decription %><br />
    <%= f.text_area :decription %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
